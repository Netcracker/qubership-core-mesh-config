{{- if eq .Values.SERVICE_MESH_TYPE "Istio" }}
apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: hcm-global-settings
spec:
  targetRefs:
    - name: {{ .Values.ISTIO_PUBLIC_GATEWAY_NAME }}
      kind: Gateway
      group: gateway.networking.k8s.io
    - name: {{ .Values.ISTIO_PRIVATE_GATEWAY_NAME }}
      kind: Gateway
      group: gateway.networking.k8s.io
    - name: {{ .Values.ISTIO_EGRESS_GATEWAY_NAME }}
      kind: Gateway
      group: gateway.networking.k8s.io
    - name: {{ .Values.ISTIO_WAYPOINT_GATEWAY_NAME }}
      kind: Gateway
      group: gateway.networking.k8s.io
  configPatches:
    - applyTo: NETWORK_FILTER
      match:
        listener:
          filterChain:
            filter:
              name: envoy.filters.network.http_connection_manager
      patch:
        operation: MERGE
        value:
          typed_config:
            '@type': type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
            server_header_transformation: PASS_THROUGH
            preserve_external_request_id: true
            generate_request_id: true
            stream_idle_timeout: 120s
            request_timeout: 120s
    - applyTo: HTTP_ROUTE
      patch:
        operation: MERGE
        value:
          decorator:
            propagate: false
          route:
            timeout: 120s
    - applyTo: VIRTUAL_HOST
      patch:
        operation: MERGE
        value:
          response_headers_to_remove:
            - x-envoy-upstream-service-time
            - x-envoy-decorator-operation
            - x-envoy-peer-metadata
            - x-envoy-peer-metadata-id
            - x-envoy-attempt-count
            - x-envoy-expected-rq-timeout-ms
            - x-envoy-external-address
            - x-envoy-internal
            - x-envoy-original-dst-host
            - x-envoy-overloaded
            - x-envoy-retry-on
            - x-envoy-upstream-healthchecked-cluster
            - x-envoy-upstream-rq-timeout-ms
            - x-envoy-upstream-service-cluster
            - x-envoy-upstream-service-zone
            - server
{{- end }}