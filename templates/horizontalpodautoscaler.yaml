{{- if eq .Values.deploy_feature_flags.core.serviceMeshType "ISTIO" }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: '{{ .Values.ISTIO_PUBLIC_GATEWAY_NAME }}-istio'
  labels:
    app.kubernetes.io/part-of: {{ .Values.APPLICATION_NAME }}
    app.kubernetes.io/managed-by: 'Helm'
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: '{{ .Values.ISTIO_PUBLIC_GATEWAY_NAME }}-istio'
  minReplicas: {{ default 2 .Values.HPA_MIN_REPLICAS }}
  maxReplicas: {{ default 10 .Values.HPA_MAX_REPLICAS }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ int (divf (mulf (default 75 .Values.HPA_AVG_CPU_UTILIZATION_TARGET_PERCENT) (include "to_millicores" .Values.PUBLIC_GW_CPU_LIMIT)) (include "to_millicores" .Values.CPU_REQUEST)) }}
  behavior:
    scaleUp:
      stabilizationWindowSeconds: {{ default 0 .Values.HPA_SCALING_UP_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_UP_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_UP_PERCENT_VALUE (ge (int .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_UP_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_UP_PODS_VALUE (ge (int .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS) 0) }}
        - type: Pods
          value: {{ .Values.HPA_SCALING_UP_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS }}
{{- end }}
    scaleDown:
      stabilizationWindowSeconds: {{ default 300 .Values.HPA_SCALING_DOWN_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_DOWN_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_DOWN_PERCENT_VALUE (ge (int .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_DOWN_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_DOWN_PODS_VALUE (ge (int .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS) 0)}}
        - type: Pods
          value: {{ .Values.HPA_SCALING_DOWN_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS }}
{{- end }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: '{{ .Values.ISTIO_PRIVATE_GATEWAY_NAME }}-istio'
  labels:
    app.kubernetes.io/part-of: {{ .Values.APPLICATION_NAME }}
    app.kubernetes.io/managed-by: 'Helm'
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: '{{ .Values.ISTIO_PRIVATE_GATEWAY_NAME }}-istio'
  minReplicas: {{ default 2 .Values.HPA_MIN_REPLICAS }}
  maxReplicas: {{ default 10 .Values.HPA_MAX_REPLICAS }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ int (divf (mulf (default 75 .Values.HPA_AVG_CPU_UTILIZATION_TARGET_PERCENT) (include "to_millicores" .Values.PRIVATE_GW_CPU_LIMIT)) (include "to_millicores" .Values.CPU_REQUEST)) }}
  behavior:
    scaleUp:
      stabilizationWindowSeconds: {{ default 0 .Values.HPA_SCALING_UP_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_UP_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_UP_PERCENT_VALUE (ge (int .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_UP_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_UP_PODS_VALUE (ge (int .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS) 0) }}
        - type: Pods
          value: {{ .Values.HPA_SCALING_UP_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS }}
{{- end }}
    scaleDown:
      stabilizationWindowSeconds: {{ default 300 .Values.HPA_SCALING_DOWN_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_DOWN_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_DOWN_PERCENT_VALUE (ge (int .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_DOWN_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_DOWN_PODS_VALUE (ge (int .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS) 0)}}
        - type: Pods
          value: {{ .Values.HPA_SCALING_DOWN_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS }}
{{- end }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: '{{ .Values.WAYPOINT_NAME }}'
  labels:
    app.kubernetes.io/part-of: {{ .Values.APPLICATION_NAME }}
    app.kubernetes.io/managed-by: 'Helm'
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: '{{ .Values.WAYPOINT_NAME }}'
  minReplicas: {{ default 1 .Values.HPA_MIN_REPLICAS }}
  maxReplicas: {{ default 5 .Values.HPA_MAX_REPLICAS }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ default 75 .Values.HPA_AVG_CPU_UTILIZATION_TARGET_PERCENT }}
  behavior:
    scaleUp:
      stabilizationWindowSeconds: {{ default 0 .Values.HPA_SCALING_UP_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_UP_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_UP_PERCENT_VALUE (ge (int .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_UP_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_UP_PODS_VALUE (ge (int .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS) 0) }}
        - type: Pods
          value: {{ .Values.HPA_SCALING_UP_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_UP_PODS_PERIOD_SECONDS }}
{{- end }}
    scaleDown:
      stabilizationWindowSeconds: {{ default 300 .Values.HPA_SCALING_DOWN_STABILIZATION_WINDOW_SECONDS }}
      selectPolicy: {{ if .Values.HPA_ENABLED }}{{ default "Max" .Values.HPA_SCALING_DOWN_SELECT_POLICY }}{{ else }}Disabled{{ end }}
      policies:
{{- if and .Values.HPA_SCALING_DOWN_PERCENT_VALUE (ge (int .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS) 0) }}
        - type: Percent
          value: {{ .Values.HPA_SCALING_DOWN_PERCENT_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PERCENT_PERIOD_SECONDS }}
{{- end }}
{{- if and .Values.HPA_SCALING_DOWN_PODS_VALUE (ge (int .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS) 0)}}
        - type: Pods
          value: {{ .Values.HPA_SCALING_DOWN_PODS_VALUE }}
          periodSeconds: {{ .Values.HPA_SCALING_DOWN_PODS_PERIOD_SECONDS }}
{{- end }}
{{- end }}

{{- end }}